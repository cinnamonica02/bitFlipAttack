from setuptools import setup, find_packages

setup(
    name="bitflip_attack",
    version="0.1.0",
    description="Bit flip attacks on privacy-sensitive models",
    author="BitFlip Attack Contributors",
    packages=find_packages(),
    python_requires=">=3.8",
) 